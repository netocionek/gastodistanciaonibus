distancia = float(input('Qual a distância da viagem?'))
print('A distância percorrida será de {}KM '.format(distancia))

if distancia > 200:
  preco = distancia * 0.45
else:
  preco = distancia * 0.50
  
 print('O total pago pela passagem será de R${}! '.format(preco))
 
 
